- name: Create Jenkins Job
  hosts: localhost
  connection: local
  vars:
    jobs:
      - roboshop-terraform
      - cart
      - catalogue
      - user
      - shipping
      - payment
      - frontend
      - dispatch
  roles:
    - jenkins-jobs